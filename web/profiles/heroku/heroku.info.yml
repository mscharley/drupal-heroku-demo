name: Drupal@Heroku
type: profile
description: 'Drupal optimised for running on Heroku'
core: 8.x

distribution:
  name: Drupal@Heroku

dependencies:
  - node
  - block
# We use monolog for watchdog instead of dblog.
  - monolog
  - page_cache
  - dynamic_page_cache
themes:
  - bartik
  - seven
